@model IEnumerable<Quran.Models.FeedbackDO>
@{
    ViewBag.Title = "Get Feedback | Learn Quran with E-Tutor";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<script>
    function DeleteFeedback(FeedbackID) {
        $.ajax({
            url: '/Admin/DeleteFeedback',
            type: 'Get',
            datatype: 'Json',
            data: { 'FeedbackID': FeedbackID },
            success: function (result) {
                if (result > 0) {
                    alert("Feedback has been deleted.");
                    window.location.href = '/Admin/GetFeedback';
                }
                else {
                    alert("Sorry! Try again.");
                }
            },
            error: function () {
                alert("Sorry! We can't delete due to some internal error.");
            }
        });
    }
</script>

<div id="banner-wrapper">
    <div id="banner" class="box container">
        <div class="row">
            <div class="col-md-12" style="width:100%;margin:0 auto;overflow-x:auto;">
                <table class="rest" border="0" style="width: 100%;text-align: center;color: black;font-size: 88%;">
                    <thead style="font-weight: 600;">
                        <tr style="color:white;">
                            <td>Serial</td>
                            <td>Name</td>
                            <td>Country</td>
                            <td>Message</td>
                            <td>Action</td>
                        </tr>
                    </thead>
                    <tbody>
                        @{int count = 1; }
                        @foreach (var Item in Model)
                        {
                            <tr>
                                <td>@count</td>
                                <td>@Item.Name</td>
                                <td>@Item.Country</td>
                                <td>@Item.Message</td>
                                <td><a href="#" class="btn btn-danger btn-sm" title="Delete" onclick='confirmAction("Are you sure you want to delete this feedback?", function(){ DeleteFeedback("@Item.ID"); }); return false;'><i class="fa fa-trash-o"></i></a></td>
                            </tr>
                            count++;
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

